A comment trailing the closing parenthesis of an import block.

-- rparen_trailing.input --
package p

import (
	"a"
) // c

x: 1
-- out/imports/rparen_trailing.attachment --
ImportDecl(1 specs): "c" (doc=false line=true position=5)
-- out/imports/rparen_trailing.roundtrip --
package p

import (
	"a"
) // c

x: 1
