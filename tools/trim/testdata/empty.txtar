Don't remove empty structs.
-- in.cue --
deployment: [string]~(ID,_): {}

deployment: alertmanager: {
	empty: {}
	volumes: [{
		name:     "alertmanager"
		emptyDir: {}
	}]
}
-- out/trim --
== in.cue
deployment: [string]~(ID,_): {}

deployment: alertmanager: {
	empty: {}
	volumes: [{
		name:     "alertmanager"
		emptyDir: {}
	}]
}
